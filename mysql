<!DOCTYPE html>
<html>
<body>

<?php
$servername = "mysql6.000webhost.com";
$username = "a2966690_sachin";
$password = "sachin123";
$dbname = "a2966690_sales";


if(isset($_GET["account"])) {
	$var1 = $_GET["account"];
// Create connection
$conn = new mysqli($servername, $username, $password, $dbname);
// Check connection
if ($conn->connect_error) {
     die("Connection failed: " . $conn->connect_error);
} 

$sql = "SELECT Name,AccNumber,Source,Site,Email FROM Account where Name = '$var1'";

$result = $conn->query($sql);

if($result)
{
	 $account_list = array();
 while ($row = $result->fetch_assoc()) {
	 $account_list[] = array("Name"=>$row["Name"],"AccNumber"=>$row["AccNumber"],"Source"=>$row["Source"],"Site"=>$row["Site"],"Email"=>$row["Email"],"Result"=>"Account Exist in the My Sql DB","Error"=>"0");
 }
	$account_json = json_encode($account_list);
	echo $account_json;

$conn->close();
}
else{
	echo  "dd";
echo  json_encode(array("Result"=>"Account Does Not Exist in the My Sql DB","Error"=>"1"));
}
}
else{
echo  json_encode(array("Result"=>"Account Does Not Exist in the My Sql DB","Error"=>"1"));
}



?>  

</body>
</html>
